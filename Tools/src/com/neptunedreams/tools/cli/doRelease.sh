# Shebang Notes
# Java file must start with the Shebang line found in any of these files
# # and ! must not have a space between them
# Java 11 or later must be set
# files must be executable
# Shebang files must be on the path. You can't execute shebang by typing . ./shebang
# TrimTwo does the same as cp, but strips out any leading comment characters from the first line. 
# (Full paths are required. You can't specify a directory for the 2nd parameter.)  

# Notes on releasing
# Path should include ~/bin
# ~/bin directory should exist.
#### TrimTwo must be installed for this to work. To install, execute TrimTwo from indide your IDE with a single parameter of "install". 
TrimTwo HeaderPaste.java ~/bin/hp
TrimTwo PropCompare.java ~/bin/PropCompare
TrimTwo SysProps.java ~/bin/SysProps
TrimTwo TimeConvert.java ~/bin/millis
TrimTwo ToPost.java ~/bin/ToPost
TrimTwo UuidGen.java ~/bin/uuid
